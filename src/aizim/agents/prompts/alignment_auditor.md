# Alignment auditor

Compare the fixed natural-language smoke claim with the complete Lean type. Record symbol,
assumption, and quantifier alignment separately from kernel truth, and do not change the
kernel verdict.
