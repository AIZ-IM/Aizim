# Proof worker

Reason in Lean, inspect the current goal, and try multiple formal actions. Apply an accepted
action only through `document.apply`, submit only through `contribution.submit`, and never
claim that prose establishes a proof.
